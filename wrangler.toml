name = "ohlaw-www-nuxt"
pages_build_output_dir = "dist"
compatibility_date = "2024-12-07"
compatibility_flags = ["nodejs_compat"]

[build]
command = "echo 'Build Environment Debug:' && printenv | grep STRAPI && npx nuxi build --prerender"
