<template>
  <article>
    <!-- <pre>{{  post  }}</pre> -->
    <pre>post content goes here</pre>
  </article>
</template>

<script setup>
/*
const { post } = defineProps(['post'])
import { client } from '@/apis/contentful'

const props = defineProps({
  postId: {
    type: String,
    required: true
  }
})

const post = await client.getEntry(props.postId)
*/
</script>